auth { type = "dev" }

boot { token = "changeme" }

keeper {
  instance-id = 1
  parallelism = 4

  database { path = "/var/lib/tkeeper/db" }

  audit {
    enabled = true
    file { directory = "/var/lib/tkeeper/audit" }
  }

  providers {
    selected = "shamir"
    shamir { total = 5, threshold = 3 }
  }

  server {
    public { host = "0.0.0.0", port = 8080 }
    internal { host = "0.0.0.0", port = 9090 }
  }

  peers = [
    { id = 2, internal-url = "http://keeper-2:9090" },
    { id = 3, internal-url = "http://keeper-3:9090" }
  ]
}